const $=id=>document.getElementById(id);async function go(){const r=await fetch('/api/admin/login',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({username:$('username').value,password:$('password').value})});const j=await r.json();if(!r.ok){$('msg').textContent=j.error||'Login failed';return}location.href='/admin'}$('login').onclick=go;$('password').addEventListener('keydown',e=>{if(e.key==='Enter')go()});
